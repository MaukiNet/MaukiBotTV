name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: test-runner-2
    steps:
    - uses: actions/checkout@v3
    - run: pwd
    - run: npm install
    - run: npm run build
    - run: screen -L -S maukibottv -d -m node /home/miaurizius/GitHub-Runner/_work/MaukiBotTV/MaukiBotTV/dist/index.js
